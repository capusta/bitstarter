<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Update Profile</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <% include _headers.ejs %>
      <link rel="stylesheet" href="/bootstrap-responsive.css" />

      <style type="text/css">
          @media (min-width: 980px) {
              body {
                  padding-top: 60px;
                  border: 5px;
              }
          }
      </style>

  </head>
  <body>
     <% include _navbar.ejs %>

    <div class="container-fluid">

        <div id="logo" class="row-fluid cool-font">
            <h4><%= user.username %> profile update</h4>
            <br> <% if (message) { %>
            <p><%= message %></p>
            <% } %>
        </div>
        <div class="row-fluid cool-font">
            <form class="offset1" action="/settings" method="post">
                <div class="row-fluid controls controls-row">
                    <div class="span3">Name:</div>
                    <input type="text" name="nameUpdate" value="<%= user.name %>">
                </div>
                <div class="controls controls-row">
                    <div class="span3">Email:</div>
                    <input type="text" name="emailUpdate" value="<%= user.email %>">
                </div>
                <div class="controls controls-row">
                    <div class="span3">BitMessege (optional): </div>
                    <input type="text" name="bitmessege" value="<%= user.bitMessegeAddr %>">
                </div>
                <div class="controls controls-row">
                    <div class="span3">Address 1:</div>
                    <input type="text" name="addrOneUpdate" value="<%= user.addressOne %>">
                </div>
                <div class="controls controls-row">
                    <div class="span3">Address (Optional)</div>
                    <input type="text" name="addrTwoUpdate" value="<%= user.addressTwo %>">
                </div>
                <div class="controls controls-row">
                    <div class="span3">Refund Bitcoin Address: </div>
                    <input type="text" name="homeBTCUpdate" value="<%= user.homeBTC %>">
                </div>
            <input type="submit" class="btn btn-info btn-mini" value="update">
            </form>
        </div>

    </div>
     <script>
         $( "#logo").addClass('animated fadeInLeft');

     </script>
  </body>
</html>
